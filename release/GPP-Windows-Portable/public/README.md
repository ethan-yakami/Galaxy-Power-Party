`public/` no longer hosts runtime HTML/CSS/JS entry files.

Current purpose:
- `public/portraits/`: portrait image assets served at `/portraits/*`

If you need to change frontend pages or browser scripts, edit `src/client/` instead.
